---
title: Environments
description: Sandbox, test, and production environments for the Tolka API — base URLs, key types, and supported language pairs.
sidebar:
  order: 4
---

import { Aside } from '@astrojs/starlight/components';

Tolka serves test and production traffic from the same base URL. The key prefix on each request determines which path it takes through the pipeline.

## Environment summary

| Environment | Base URL | Key prefix | Suitable for |
|-------------|----------|-----------|--------------|
| **Test** | `https://api.tolka.health` | `sk_test_...` | Integration development, CI pipelines. Request via [tolka.health](https://tolka.health). |
| **Production** | `https://api.tolka.health` | `sk_live_...` | Live patient-facing deployments. Issued after a pilot agreement. |

Test and production responses go through the same translation pipeline — only billing, quota, and `key_prefix` audit attribution differ. There is no quality-of-output difference between key types.

---

## Supported language pairs

| Source | Target | Code pair | Notes |
|--------|--------|-----------|-------|
| Norwegian Bokmål | English | `nb → en` | Primary supported pair |
| English | Norwegian Bokmål | `en → nb` | |
| Arabic | Norwegian Bokmål | `ar → nb` | |
| Arabic | English | `ar → en` | |
| Somali | Norwegian Bokmål | `so → nb` | |
| Somali | English | `so → en` | |
| Polish | Norwegian Bokmål | `pl → nb` | |
| Polish | English | `pl → en` | |
| Tigrinya | Norwegian Bokmål | `ti → nb` | |
| Dari | English | `prs → en` | |

Language codes follow **ISO 639-1**. Pass them in `source_language` and `target_language`.

:::caution[Low-resource language pairs]
Some language pairs have adjusted confidence baselines. Always rely on `risk` as your primary UI signal rather than the raw `confidence` float.
:::

---

## Clinical domains

Pass one of these values in the `domain` field:

| Value | Clinical context |
|-------|-----------------|
| `emergency` | Emergency and acute care — prioritises speed and unambiguous terminology |
| `gp` | General practice consultations |
| `mental_health` | Mental health and psychiatric settings |
| `pediatric` | Paediatric care |
| `pharmacy` | Medication instructions and pharmaceutical context |

---

## What's next

- [Rate limits and retry strategy →](/getting-started/rate-limits/)
- [Going to production checklist →](/getting-started/going-to-production/)

